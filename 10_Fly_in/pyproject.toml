[project]
name = "fly-in"
version = "0.1.0"
description = "Drone routing system that navigates multiple drones through connected zones"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "flake8>=7.3.0",
    "mypy>=1.20.0",
]
